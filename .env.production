# Same-origin API calls; Vercel rewrites /auth,/users,/api to Nest (Railway).
VITE_API_BASE=

# Keep false for real role-based auth. Set true only for UI portal previews.
VITE_ENABLE_ROLE_SWITCHER=false
