VITE_ENABLE_ROLE_SWITCHER=true

# Primary backend: NestJS (ds-back-master), same service as Railway production.
# Local Nest (needs Postgres/MQTT/.env in ds-back-master):
# VITE_API_PROXY_TARGET=http://127.0.0.1:3000
#
# Deployed Nest (live kits/readings):
VITE_API_PROXY_TARGET=https://ds-back-production.up.railway.app
